<Window x:Class="DataTriggers.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:DataTriggers"
        Title="MainWindow" Height="300" Width="525">
    <StackPanel RenderTransformOrigin="0.316,0.146">
        <StackPanel.Resources>

            <!-- <Style TargetType="{x:Type Image}" x:Key="myImage">
                <Setter Property="Width" Value="70" />
                <Style.Triggers>
                    <DataTrigger Binding="{Binding Path=isFound}" Value="False">
                        <Setter Property="Source" Value="not_available.png" />
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Path=isFound}" Value="True">
                        <Setter Property="Source" Value="{Binding Path=pathToImage}" />
                    </DataTrigger>                    
                </Style.Triggers>
            </Style>
            -->
        </StackPanel.Resources>
        <!-- <Image Style="{StaticResource myImage}" /> -->


        <local:ImagesWithTitles Background="AntiqueWhite" Szerokosc="800" ImagesSource="{Binding}" Height="250" Margin="35,0" RenderTransformOrigin="-0.336,1.161" />
      


        <!-- <Image Style="{StaticResource myImage}" /> -->
        <!--<local:ImagesWithTitles ImagesSource="{Binding}" />
        <local:ImagesWithTitles ImagesSource="{Binding}" />-->


    </StackPanel>
</Window>
