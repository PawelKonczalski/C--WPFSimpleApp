<Window x:Class="DataContextAndTheINotifyPropertyChangedInterface.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DataContextAndTheINotifyPropertyChangedInterface"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <local:InkEditingModes x:Key="editingModes" />
    </Window.Resources>
    <DockPanel x:Name="dockpanelTest" DataContext="{StaticResource editingModes}">
        <StackPanel Width="80">
        <Button x:Name="select" Content="Select" Click="select_Click"/>
        <Button x:Name="draw" Content="Draw" Click="draw_Click"/>
        </StackPanel>
            <InkCanvas EditingMode="{Binding Path=EditingMode}">
            
            </InkCanvas>
    </DockPanel>
</Window>
